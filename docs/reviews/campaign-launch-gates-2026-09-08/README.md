# Campaign launch gates

Code 0149e98 compared with parent 976d894. Release, CC_BUILD_CLIENT=OFF, CC_BUILD_BENCHMARKS=ON, strict warnings as errors. All 76 headless tests passed.

The shared plan retains two stages: preparation eligibility, then departure after leaders and supplies are prepared. The diagnostic lists held resources and IDs at inspection time. The real daily-loop test starts without leaders; it names them and launches when the cooldown is lifted, with an otherwise matched cooldown control remaining idle. Unit cases cover each launch gate and the deep-wyrm age exception, plus byte-for-byte read-only inspection.

The short probe compared 640 annual state hashes across eight schemas and two seeds. The long probe compared 1,200 more hashes from two 600-year schema-59 histories. Every hash matched. Compile each probe against the respective build's libcrownless_sim.a with its src include directory and -lm.

`crownless_sim_runner --seed 0x5eed0001 --years 1 --detail` produced detail.txt. The launch snapshot reports the existing lifetime departure count and current readiness, with the diplomacy schedule controlling when preparation actually runs. Coalition formation, reserve-aware supply availability, and ritual gates remain diagnostic work under #266.
